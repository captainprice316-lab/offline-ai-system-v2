# ── Core ML ───────────────────────────────────────────────────────────────────
faster-whisper==1.0.3
transformers>=4.40.0
# Windows RTX 5060 (Blackwell): install torch BEFORE this file with:
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu126
# Then run: pip install -r requirements.txt
torch>=2.6.0
torchaudio>=2.6.0

# ── Audio processing ──────────────────────────────────────────────────────────
librosa==0.10.1
soundfile==0.12.1
noisereduce==3.0.2
silero-vad>=5.1                  # 4.0.0 was GitHub-only, never on PyPI; v5+ has identical API

# ── Language detection ────────────────────────────────────────────────────────
fasttext-wheel==0.9.2           # pre-built wheel works on macOS/Linux too (native fasttext fails to build without pybind11 dev)

# ── NLP / Translation ─────────────────────────────────────────────────────────
sentencepiece==0.2.0
sacremoses==0.1.1
protobuf==3.20.3                # IndicTrans2 requires <=3.x

# ── IndicTrans2 dependencies ──────────────────────────────────────────────────
# mosestokenizer                # required by IndicTrans2 — skip on Windows (needs Perl); Dogri (doi) only
indic-nlp-library==0.92         # required by IndicTrans2

# ── Metrics ───────────────────────────────────────────────────────────────────
jiwer                           # WER / CER (Tier 2 metrics)
sacrebleu                       # BLEU / chrF / TER (Tier 2 metrics)

# ── Export ────────────────────────────────────────────────────────────────────
python-docx                     # Word document export
reportlab                       # PDF export

# ── UI ────────────────────────────────────────────────────────────────────────
streamlit==1.34.0
altair==5.3.0                   # Dashboard charts — pin: 5.4+ emits Vega specs streamlit 1.34 can't render
streamlit-mic-recorder          # microphone recording widget (works on streamlit 1.34)
pandas==2.2.2

# ── Utilities ─────────────────────────────────────────────────────────────────
pyyaml==6.0.1
numpy==1.26.4
setuptools<80                   # pkg_resources (used by librosa/noisereduce) removed in setuptools 80+
psutil                          # system memory stats (used in sidebar)

# ── NLP (NER for ISUM / geo extraction) ───────────────────────────────────────
spacy
# en_core_web_sm installed separately (GitHub SSL workaround, see setup notes)
folium                          # geo_module.py map building
geonamescache                   # geo_module.py city fallback (~32k cities)
plotly                          # MAP tab (Plotly Scattergeo) + network/timeline charts

# ── Windows RTX 5060 (Blackwell sm_120) setup order ───────────────────────────
# 1. py -3.11 -m venv venv
# 2. python -m pip install pip-system-certs   ← fixes SSL on Windows (use python -m pip, not pip directly)
# 3. pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu128   ← cu128 required for Blackwell
# 4. pip install -r requirements.txt
# 5. python -m pip install https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.8.0/en_core_web_sm-3.8.0-py3-none-any.whl
# faster-whisper uses CUDA via CTranslate2 automatically when device: cuda in config.yaml
# transformers models (NLLB, Qwen, MMS-LID) use device: cuda from config.yaml
